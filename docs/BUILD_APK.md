# 📱 Сборка Android APK для ONDA

Подробная инструкция по созданию Android APK файла с оптимизацией для минимального размера.

---

## 🎯 Архитектура приложения

**ONDA Android** состоит из:
1. **React PWA** - веб-приложение (фронтенд)
2. **Android WebView** - нативная обёртка
3. **Health Connect Bridge** - интеграция с Health Connect API
4. **Bluetooth Bridge** - поддержка HRM трекеров
5. **CDN Audio System** - аудио файлы загружаются из Supabase Storage

### Размер APK:
- **Без CDN**: ~250MB (включая все аудио)
- **С CDN**: ~5-10MB (аудио загружается при первом использовании)

---

## 📋 Требования

### Программное обеспечение:
- ✅ **Android Studio** (latest stable) - [Скачать](https://developer.android.com/studio)
- ✅ **JDK 11 или выше**
- ✅ **Node.js 18+** и npm

### Система:
- Windows 10/11, macOS 10.14+, или Linux
- Минимум 8GB RAM
- ~10GB свободного места на диске

---

## 🚀 Пошаговая инструкция

### **Шаг 1: Клонировать проект**

```bash
# Клонируйте репозиторий
git clone <your-repo-url>
cd onda-app

# Установите зависимости
npm install
```

### **Шаг 2: Подготовить веб-приложение**

```bash
# Автоматическая подготовка (рекомендуется)
npm run prepare:android
```

**Что делает этот скрипт:**
1. Собирает React приложение (`npm run build`)
2. Копирует `dist/` в `android-webview/app/src/main/assets/`
3. **Исключает аудио файлы** (они загружаются из CDN)
4. Проверяет что все необходимые файлы на месте

**Ожидаемый вывод:**
```
🚀 ONDA: Подготовка Android APK
================================
📦 Шаг 1: Сборка веб-приложения
   Запуск: npm run build
✅ Веб-приложение собрано

📁 Шаг 2: Создание assets директории
✅ Assets директория готова

📋 Шаг 3: Копирование веб-файлов (БЕЗ аудио)
🗑️  Удаление аудио файлов (используется CDN)
   ✓ Удалено: practices p1/
   ✓ Удалено: adaptive-practices/
✅ Аудио файлы исключены

🔍 Шаг 4: Проверка скопированных файлов
   ✓ index.html
   ✓ assets
   ✓ locales

📊 Размер assets: 3.2M

✅ Подготовка завершена!
```

---

### **Шаг 3: Открыть проект в Android Studio**

1. Запустите Android Studio
2. Выберите **File → Open**
3. Откройте папку **`android-webview/`**
4. Дождитесь синхронизации Gradle (~2-5 минут)

**Возможные проблемы:**
- ❌ "Gradle sync failed" → Проверьте интернет-соединение
- ❌ "SDK not found" → Установите Android SDK через Android Studio

---

### **Шаг 4: Создать Keystore (один раз)**

Для публикации в Google Play нужен **release keystore**:

```bash
cd android-webview/app

# Создайте keystore файл
keytool -genkey -v \
  -keystore onda-release.keystore \
  -alias onda \
  -keyalg RSA \
  -keysize 2048 \
  -validity 10000
```

**Введите данные:**
```
Enter keystore password: [ваш_пароль]
Re-enter new password: [ваш_пароль]
What is your first and last name? [Ваше имя или название компании]
What is the name of your organizational unit? [Название отдела]
What is the name of your organization? [Название организации]
What is the name of your City or Locality? [Город]
What is the name of your State or Province? [Регион]
What is the two-letter country code for this unit? [RU]
```

**⚠️ ВАЖНО: Сохраните пароли!**
- Keystore password
- Key password

Без них вы не сможете обновить приложение в Google Play!

---

### **Шаг 5: Настроить подпись (один раз)**

#### Вариант A: Через Android Studio (рекомендуется)

1. **Build → Generate Signed Bundle / APK**
2. Выберите **APK**
3. Click **Create new...**
4. Укажите путь к keystore файлу
5. Введите пароли
6. Сохраните конфигурацию

#### Вариант B: Через build.gradle (для CI/CD)

Добавьте в `android-webview/app/build.gradle`:

```gradle
android {
    ...
    
    signingConfigs {
        release {
            storeFile file('onda-release.keystore')
            storePassword 'ВАШ_KEYSTORE_PASSWORD'
            keyAlias 'onda'
            keyPassword 'ВАШ_KEY_PASSWORD'
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
```

**⚠️ Безопасность:** Не коммитьте пароли в Git! Используйте переменные окружения:

```gradle
signingConfigs {
    release {
        storeFile file('onda-release.keystore')
        storePassword System.getenv("KEYSTORE_PASSWORD")
        keyAlias 'onda'
        keyPassword System.getenv("KEY_PASSWORD")
    }
}
```

---

### **Шаг 6: Собрать APK**

#### **A. Через Android Studio (GUI)**

1. **Build → Generate Signed Bundle / APK**
2. Выберите **APK**
3. Выберите ваш keystore файл
4. Введите пароли
5. Выберите **release** build variant
6. **Finish**

**APK будет здесь:**
```
android-webview/app/build/outputs/apk/release/app-release.apk
```

#### **B. Через командную строку**

```bash
cd android-webview

# Debug APK (для тестирования, НЕ для публикации)
./gradlew assembleDebug
# APK: app/build/outputs/apk/debug/app-debug.apk

# Release APK (для публикации)
./gradlew assembleRelease
# APK: app/build/outputs/apk/release/app-release.apk
```

**Ожидаемое время:** 2-5 минут (первая сборка может занять до 10 минут)

---

## 📊 Проверка размера APK

```bash
# Размер APK
ls -lh app/build/outputs/apk/release/app-release.apk

# Детальный анализ
cd android-webview
./gradlew analyzeReleaseApkSize
```

**Ожидаемый размер:**
- **5-10 MB** - отлично! (только веб-приложение, без аудио)
- **>50 MB** - проверьте, что аудио файлы исключены из assets

---

## 🧪 Тестирование APK

### На реальном устройстве:

```bash
# Подключите Android устройство через USB
# Включите "Отладка по USB" в настройках разработчика

# Установите APK
adb install -r app/build/outputs/apk/release/app-release.apk

# Или просто скопируйте APK на телефон и установите вручную
```

### На эмуляторе:

1. Запустите эмулятор в Android Studio
2. Drag & drop APK файл на экран эмулятора

### Что проверить:

- [ ] Приложение запускается
- [ ] Отображается главный экран
- [ ] Можно войти/зарегистрироваться
- [ ] Аудио треки загружаются из CDN (первый запуск)
- [ ] Аудио воспроизводится
- [ ] Health Connect работает (если установлен)
- [ ] Bluetooth permissions запрашиваются

---

## 🐛 Troubleshooting

### ❌ **"Белый экран при запуске"**

**Причина:** assets не скопированы

**Решение:**
```bash
npm run prepare:android
```

Проверьте что файлы есть:
```bash
ls -la android-webview/app/src/main/assets/
```

### ❌ **"Gradle sync failed"**

**Причина:** Неправильная версия JDK или интернет-проблемы

**Решение:**
1. Установите JDK 11: **File → Project Structure → SDK Location**
2. Проверьте интернет-соединение
3. Попробуйте: **File → Invalidate Caches → Invalidate and Restart**

### ❌ **"Signing config not found"**

**Причина:** Keystore файл не найден

**Решение:**
1. Проверьте путь к keystore в `build.gradle`
2. Убедитесь что файл существует: `ls android-webview/app/onda-release.keystore`

### ❌ **"APK размер >100MB"**

**Причина:** Аудио файлы включены в APK

**Решение:**
```bash
# Проверьте assets
ls -la android-webview/app/src/main/assets/

# Должно НЕ быть:
# - practices p1/
# - adaptive-practices/

# Пересоберите
npm run prepare:android
cd android-webview
./gradlew clean assembleRelease
```

### ❌ **"Аудио не воспроизводится"**

**Причина:** Проблемы с CDN или кешированием

**Решение:**
1. Проверьте интернет-соединение на устройстве
2. Проверьте логи:
   ```bash
   adb logcat | grep "AudioCache"
   adb logcat | grep "WebView Console"
   ```
3. Очистите кеш приложения в настройках Android

---

## 📤 Публикация в Google Play

### 1. Создайте аккаунт разработчика

- Перейдите на [Google Play Console](https://play.google.com/console)
- Зарегистрируйтесь ($25 единоразовый платеж)

### 2. Создайте приложение

1. **Create app** в Play Console
2. Заполните информацию о приложении
3. Загрузите скриншоты (минимум 2 на каждый форм-фактор)
4. Напишите описание (EN + RU)

### 3. Загрузите APK/AAB

**Рекомендация:** Используйте Android App Bundle (AAB) вместо APK:

```bash
cd android-webview
./gradlew bundleRelease
# AAB: app/build/outputs/bundle/release/app-release.aab
```

**Преимущества AAB:**
- Google Play автоматически оптимизирует APK для каждого устройства
- Меньший размер скачивания для пользователей

### 4. Заполните остальные разделы

- **Store listing:** Описание, скриншоты, иконки
- **Content rating:** Пройдите опрос IARC
- **Pricing:** Бесплатно/платно
- **Release:** Создайте первый релиз

### 5. Опубликуйте!

- Отправьте на review (~2-7 дней)
- После approval приложение появится в Google Play

---

## 🔄 Обновление приложения

При обновлении **обязательно увеличивайте** version в `build.gradle`:

```gradle
defaultConfig {
    ...
    versionCode 2  // Увеличьте на 1
    versionName "1.1"  // Обновите версию
}
```

**versionCode** - внутренний номер (целое число)  
**versionName** - отображаемая версия (строка)

---

## 📦 Итоговый чеклист

Перед сборкой финального APK:

- [ ] Запустил `npm run prepare:android`
- [ ] Проверил что аудио файлы НЕ в assets
- [ ] Создал release keystore
- [ ] Настроил signing config
- [ ] Собрал release APK
- [ ] Протестировал на реальном устройстве
- [ ] Проверил размер APK (<15MB)
- [ ] Проверил что аудио загружается из CDN
- [ ] Проверил Health Connect integration
- [ ] Увеличил versionCode/versionName

---

## 🎯 Итоговые команды

```bash
# Полный процесс сборки
cd /path/to/onda-app

# 1. Подготовить веб-приложение
npm run prepare:android

# 2. Собрать APK
cd android-webview
./gradlew assembleRelease

# 3. Проверить результат
ls -lh app/build/outputs/apk/release/app-release.apk

# 4. Установить на устройство
adb install -r app/build/outputs/apk/release/app-release.apk
```

---

## 📚 Дополнительные ресурсы

- [Android Developer Guide](https://developer.android.com/guide)
- [WebView Best Practices](https://developer.android.com/develop/ui/views/layout/webapps/webview)
- [Google Play Publishing Guide](https://developer.android.com/distribute/best-practices/launch)
- [ProGuard Configuration](https://www.guardsquare.com/manual/configuration)

---

**Успешной публикации! 🚀**

Если возникнут проблемы - проверьте раздел Troubleshooting или создайте issue в репозитории.
