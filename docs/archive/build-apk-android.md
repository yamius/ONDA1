# 🔨 Сборка APK #45 на локальном компьютере

## 📦 Что изменилось в версии 45:

### ✅ Исправлено:
1. **Graceful handling missing HC permissions**
   - Каждое body measurement теперь обрабатывается отдельно
   - SecurityException (например, для Lean Body Mass) больше не ломает другие данные
   - Вместо ERROR выводится WARNING для ожидаемых отсутствующих разрешений

2. **Улучшенное логирование Heart Rate**
   - Добавлен вывод временного диапазона запроса (start/end)
   - Показывается количество найденных записей
   - Итоговый JSON vitals для диагностики

### 🐛 Баг из версии 43-44:
```
Error reading body measurements
SecurityException: READ_LEAN_BODY_MASS
```
**Исправлено!** Теперь отсутствие разрешения на Lean Body Mass не ломает Weight, Height, Body Fat.

---

## 🛠️ Инструкция по сборке:

### Способ 1: Автоматический скрипт (рекомендуется)

```bash
cd android-webview
./build-apk-45.sh
```

Скрипт автоматически:
- Установит versionCode 45
- Очистит предыдущие сборки
- Соберет APK
- Скопирует в `app-release-45.apk`
- Покажет инструкции по установке

---

### Способ 2: Вручную через Gradle

```bash
cd android-webview

# Обновите версию в build.gradle.kts
# versionCode = 45
# versionName = "1.0.45"

# Соберите APK
./gradlew clean
./gradlew assembleRelease

# APK будет в:
# app/build/outputs/apk/release/app-release-unsigned.apk
```

---

### Способ 3: Через Android Studio

1. Откройте проект `android-webview/` в Android Studio
2. **Build → Generate Signed Bundle / APK → APK**
3. Выберите release конфигурацию
4. Установите versionCode = 45
5. Build APK

---

## 📱 Установка и тестирование:

```bash
# Удалите старую версию
adb uninstall com.onda.app

# Установите APK #45
adb install app-release-45.apk

# Запустите приложение
adb shell am start -n com.onda.app/.MainActivity
```

---

## 🔍 Проверка изменений:

### 1. Логи должны быть чище:

**Было (версия 43-44):**
```
E HealthConnectManager: Error reading body measurements
E HealthConnectManager: SecurityException: READ_LEAN_BODY_MASS
[50+ строк stack trace]
```

**Стало (версия 45):**
```
W HealthConnectManager: No permission for Lean Body Mass (expected - user denied)
```

### 2. Проверьте Heart Rate логирование:

```bash
adb logcat -c
adb logcat | grep "Heart Rate"
```

**Вы увидите:**
```
D HealthConnectManager: readVitals: start=2025-11-18T00:00:00Z, end=2025-11-19T...
D HealthConnectManager: Heart Rate records found: 0
W HealthConnectManager: No Heart Rate records found in timeRange
```

---

## 💡 После установки APK #45:

### Если пульс все еще не показывается:

Это **ожидаемое поведение** - на телефоне нет данных о пульсе!

**Добавьте данные через Google Fit:**
1. Откройте Google Fit
2. Профиль → Добавить данные → Пульс
3. Введите 72 BPM (сегодняшняя дата)
4. Сохраните
5. Откройте ONDA → Refresh Health Connect Data

**Проверьте логи:**
```bash
adb logcat | grep "Heart Rate records"
```

**Должны увидеть:**
```
Heart Rate records found: 1    ← Данные появились!
Heart Rate value: 72
```

---

## 📊 Что работает сейчас (из ваших логов):

✅ Steps: 6426  
✅ Active Calories: 762  
✅ Sleep: sessions from Nov 12  
✅ 17/18 permissions granted  
❌ Heart Rate: **0 records** (нет данных на устройстве)

---

## 🎯 Итого:

**Версия 45** исправляет обработку ошибок и улучшает диагностику.  
Отсутствие данных о пульсе - это не баг, а отсутствие записей в Health Connect.

Добавьте данные через Google Fit/Samsung Health и все заработает! 💓
