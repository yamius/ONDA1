# 📱 Как собрать Android приложение с поддержкой фитнес-трекера

Этот проект уже включает готовую веб-часть и нативный WebView-обёртку для Android. Ниже — короткая инструкция, как собрать своё приложение, подключить Bluetooth-пульсометр и вывести ключевые биометрики на экране.

## 1) Что понадобится
- Node.js 18+ и npm
- Android Studio (или Android SDK + Gradle)
- Android-устройство с поддержкой Bluetooth Low Energy
- Любой совместимый пульсометр (Polar, Garmin, Mi Band, Wahoo и др.)

## 2) Быстрый старт (web + Android)
1. Установите зависимости и подготовьте сборку:
   ```bash
   npm install
   npm run prepare:android
   ```
   Скрипт соберёт веб-приложение и положит артефакты в `android-webview/app/src/main/assets/`.
2. Соберите APK через Gradle:
   ```bash
   cd android-webview
   ./gradlew assembleDebug
   ```
   Готовый APK появится в `android-webview/app/build/outputs/apk/debug/`.
3. Установите APK на устройство (adb или вручную) и откройте приложение.

## 3) Подключение трекера и вывод метрик
В приложении реализовано подключение BLE-пульсометров через Web Bluetooth API.
1. На главном экране откройте **⚙️ Settings** и нажмите **🔵 Connect Tracker**.
2. В системном диалоге выберите ваш трекер и подтвердите pairing.
3. После подключения появятся кнопки **✅ Tracker Connected** и **❌ Disconnect**, а ниже — карточки с данными.

### Метрики, которые выводятся
- **Heart Rate** (пульс, BPM)
- **Breathing Rate** (частота дыхания)
- **Stress**, **Energy**
- HRV surrogate, Cardiac Stability Index, Recovery Rate
- HR trend slope, HR Acceleration
- Индексы состояний: Anxiety, Relaxation, Focus, Excitement, Fatigue, Flow

## 4) Настройка под себя
- **Брендинг/иконки:** обновите ресурсы в `android-webview/app/src/main/res/`.
- **Цветовую схему и UI:** правьте компоненты в `src/` и пересоберите через `npm run prepare:android`.
- **Permissions:** список разрешений и настройки безопасности находятся в `android-webview/app/src/main/AndroidManifest.xml` и `android-webview/app/src/main/res/xml/network_security_config.xml`.

## 5) Советы по стабильности
- Перед подключением убедитесь, что трекер не занят другим приложением.
- Используйте современные Android-устройства (API 26+), чтобы Web Bluetooth работал корректно.
- Для релизной сборки выполните `./gradlew assembleRelease` и настройте подпись keystore.

Готово! Так вы получите собственное Android-приложение с реальными биометриками прямо с вашего трекера.
