# Matchfile for ONDA iOS App
# Match is used to sync code signing certificates and provisioning profiles

# Git URL for storing certificates (must be a private repository)
# This will be set via environment variable: MATCH_GIT_URL
git_url(ENV["MATCH_GIT_URL"] || "https://github.com/YOUR_USERNAME/certificates")

# Storage mode (git is recommended)
storage_mode("git")

# Type of certificates to manage (development, adhoc, appstore, enterprise)
type("appstore")

# App Identifier (Bundle ID)
app_identifier(["com.onda.app"])

# Your Apple ID username
# This will be set via environment variable: FASTLANE_USER
username(ENV["FASTLANE_USER"] || "your-apple-id@example.com")

# Team ID
# This will be set via environment variable: FASTLANE_TEAM_ID
team_id(ENV["FASTLANE_TEAM_ID"])

# For fastlane match certificates storage encryption
# This will be set via environment variable: MATCH_PASSWORD
# match_password(ENV["MATCH_PASSWORD"])
