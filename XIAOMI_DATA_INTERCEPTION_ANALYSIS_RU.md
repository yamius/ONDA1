# Анализ: Перехват данных Xiaomi Smart Band без специальной команды

## 🎯 Задача
Получать данные от Xiaomi Smart Band напрямую, без включения специальной команды в приложении трекера.

---

## 📊 Исследование существующих решений

### 1. ❌ Официальный Xiaomi SDK - НЕ СУЩЕСТВУЕТ

**Факт:** Xiaomi не предоставляет официальный SDK или API для Mi Band.

**Все решения основаны на:**
- Reverse-engineering BLE протокола
- Анализ приложения Mi Fitness через Frida/JADX
- Community-driven проекты (неофициальные)

---

### 2. ✅ Google Health Connect - УЖЕ РАБОТАЕТ У ВАС!

**Ваша текущая реализация:**
```typescript
// src/hooks/useHealthConnect.ts
// Читает данные из Health Connect
```

**Как это работает:**
```
Xiaomi Smart Band (BLE)
    ↓
Mi Fitness app (синхронизация каждые 10-30 минут)
    ↓
Google Health Connect (автоматически)
    ↓
ONDA app (useHealthConnect hook)
```

**Преимущества:**
- ✅ Официальный API от Google
- ✅ Автоматическая синхронизация
- ✅ Поддержка 19 типов данных
- ✅ Уже реализовано в ONDA

**Недостатки:**
- ⏱️ Задержка 10-30 минут (не real-time)
- 📱 Требует Mi Fitness app на телефоне
- 🔌 Зависит от интернета для синхронизации

---

### 3. 🔔 NotificationListenerService - ПЕРЕХВАТ УВЕДОМЛЕНИЙ

**Идея:** Перехватывать уведомления от Mi Fitness о достижениях/целях

**Что можно получить:**
- "Достигнуто 10,000 шагов"
- "Пульс: 72 bpm"
- "Цель сна выполнена: 8 часов"

**Реализация:**
```java
// Android NotificationListenerService
public class MiFitnessListener extends NotificationListenerService {
    @Override
    public void onNotificationPosted(StatusBarNotification sbn) {
        if (sbn.getPackageName().equals("com.mi.health")) {
            String text = sbn.getNotification().extras.getString("android.text");
            // Парсим данные из текста уведомления
            sendToWebView(text);
        }
    }
}
```

**Преимущества:**
- ✅ Официальный Android API
- ✅ Работает в фоне
- ✅ Не требует root

**Недостатки:**
- ❌ Только данные из уведомлений (не continuous)
- ❌ Зависит от настроек уведомлений Mi Fitness
- ❌ Не real-time сенсорные данные

---

### 4. 📡 BLE Direct Connection - ПРЯМОЕ ПОДКЛЮЧЕНИЕ

**Самое продвинутое решение:** Подключаться к трекеру напрямую через Bluetooth

#### **Требования:**
1. **Pairing обязателен** - без него данные недоступны
2. **Authentication key** - нужно извлечь из Mi Fitness базы данных
3. **Реализация протокола** - reverse-engineered

#### **Ключевая проблема:**
```
Mi Band 5+ использует ШИФРОВАНИЕ через Xiaomi серверы
Требуется уникальный ключ устройства (device key)
```

#### **BLE Характеристики (Mi Band):**

| Функция | UUID | Паринг нужен? |
|---------|------|---------------|
| Battery | `0xFF0C` | Нет (может работать) |
| Heart Rate | `0x2A37` | **Да** |
| Steps | `0xFF03` | **Да** |
| Control | `0xFF05` | **Да** |

**Вывод:** Без паринга доступна только информация о батарее, все health данные требуют authentication.

---

### 5. 🚀 Gadgetbridge - OPEN-SOURCE РЕШЕНИЕ

**Самый мощный вариант:** Полная замена Mi Fitness

**GitHub:** https://github.com/Freeyourgadget/Gadgetbridge

**Что даёт:**
- ✅ Полная реализация Mi Band протокола (Band 1-9)
- ✅ Real-time данные от трекера
- ✅ Не зависит от Mi Fitness app
- ✅ Не требует интернета
- ✅ Open-source (Apache 2.0)

**Архитектура:**
```
Xiaomi Smart Band (BLE)
    ↓
Gadgetbridge library (в ONDA app)
    ↓
Прямой доступ к сенсорам
```

**Поддерживаемые данные:**
- Heart rate (real-time)
- Steps
- Sleep
- SpO2
- Stress
- Battery
- Workouts

**Интеграция в ONDA:**
```kotlin
// Добавить Gadgetbridge как библиотеку
implementation 'com.github.Freeyourgadget:Gadgetbridge:master-SNAPSHOT'

// Или использовать их BLE протокол
class MiBandManager {
    fun connectToDevice(address: String) {
        // Gadgetbridge protocol implementation
    }
    
    fun subscribeHeartRate(callback: (Int) -> Unit) {
        // Real-time HR updates
    }
}
```

**Недостатки:**
- 🔧 Требует значительной интеграции
- 📦 Увеличивает размер APK (~2-3 MB)
- 🔑 Всё равно нужен первый паринг через Mi Fitness для получения auth key
- 🛠️ Поддержка требует обновлений при новых моделях Mi Band

---

### 6. 🌐 WebSocket/REST Server - ОБЛАЧНЫЙ ПОСРЕДНИК

**Идея:** Companion приложение на телефоне отправляет данные на ваш сервер

**Архитектура:**
```
Xiaomi Band → Mi Fitness → Companion App (NotificationListener) → WebSocket → ONDA Server → ONDA Web App
```

**Преимущества:**
- ✅ Работает с любого устройства (не только Android с APK)
- ✅ Можно агрегировать данные от разных источников

**Недостатки:**
- ❌ Требует постоянный серверный backend
- ❌ Зависимость от интернета
- ❌ Дополнительная задержка
- ❌ Затраты на хостинг

---

## 🎯 Рекомендации для ONDA

### **Вариант 1: ОСТАВИТЬ КАК ЕСТЬ (Health Connect) ✅**

**Почему:**
- ✅ Уже работает
- ✅ Официальный API
- ✅ Минимум кода
- ✅ Надёжно

**Улучшения:**
```typescript
// Увеличить частоту опроса Health Connect
useEffect(() => {
  const interval = setInterval(() => {
    readHealthConnectData(); // Каждые 5 минут вместо по требованию
  }, 5 * 60 * 1000);
  return () => clearInterval(interval);
}, []);
```

---

### **Вариант 2: ДОБАВИТЬ NotificationListenerService (СРЕДНИЙ)**

**Цель:** Быстрые уведомления о достижениях без ожидания синхронизации

**Реализация:**
1. Добавить `NotificationListenerService` в Android app
2. Перехватывать уведомления Mi Fitness
3. Парсить health данные из текста
4. Отправлять в WebView через JavaScript bridge

**Файлы для создания:**
```
android-webview/app/src/main/java/com/onda/app/MiFitnessNotificationListener.kt
```

**Преимущества:**
- ✅ Дополняет Health Connect
- ✅ Быстрее узнаём о целях/достижениях
- ✅ ~50 строк кода

**Недостатки:**
- ❌ Не real-time continuous data
- ❌ Зависит от уведомлений

---

### **Вариант 3: ИНТЕГРАЦИЯ GADGETBRIDGE (ПРОДВИНУТЫЙ)**

**Цель:** Полностью независимая работа без Mi Fitness

**Задачи:**
1. Интегрировать Gadgetbridge SDK
2. Реализовать паринг и authentication
3. Подписаться на BLE characteristics
4. Обработка real-time данных

**Преимущества:**
- ✅ Real-time данные
- ✅ Не зависит от Mi Fitness
- ✅ Полный контроль над трекером

**Недостатки:**
- ❌ 2-3 недели разработки
- ❌ Увеличение APK на 2-3 MB
- ❌ Поддержка новых моделей
- ❌ Всё равно нужен первый паринг с Mi Fitness для auth key

---

## 🏆 ФИНАЛЬНАЯ РЕКОМЕНДАЦИЯ

### **Для ONDA App:**

**Комбинированный подход (оптимальный):**

1. **Health Connect (основной источник)** ✅ УЖЕ ЕСТЬ
   - Исторические данные
   - Надёжная синхронизация
   
2. **NotificationListenerService (дополнительно)** 🆕 ДОБАВИТЬ
   - Быстрые события
   - Достижения целей
   - ~1-2 часа разработки

3. **Web Bluetooth (уже реализовано)** ✅ УЖЕ ЕСТЬ
   - Для generic BLE heart rate monitors
   - Альтернатива Xiaomi

**Не рекомендую:**
- ❌ Gadgetbridge - слишком сложно для текущих требований
- ❌ WebSocket server - ненужная инфраструктура
- ❌ Reverse-engineering BLE - нестабильно

---

## 📋 План действий (если решите добавить NotificationListener)

### **Шаг 1: Добавить NotificationListenerService**

```kotlin
// android-webview/app/src/main/java/com/onda/app/MiFitnessNotificationListener.kt
package com.onda.app

import android.service.notification.NotificationListenerService
import android.service.notification.StatusBarNotification
import android.util.Log

class MiFitnessNotificationListener : NotificationListenerService() {
    
    companion object {
        const val TAG = "MiFitnessListener"
        const val MI_FITNESS_PACKAGE = "com.mi.health"
    }
    
    override fun onNotificationPosted(sbn: StatusBarNotification) {
        if (sbn.packageName == MI_FITNESS_PACKAGE) {
            val extras = sbn.notification.extras
            val title = extras.getString("android.title") ?: ""
            val text = extras.getCharSequence("android.text")?.toString() ?: ""
            
            Log.d(TAG, "Mi Fitness notification: $title - $text")
            
            // Отправить в WebView
            MainActivity.instance?.runOnUiThread {
                MainActivity.instance?.webView?.evaluateJavascript(
                    "window.dispatchEvent(new CustomEvent('mi-fitness-notification', { detail: { title: '$title', text: '$text' } }))",
                    null
                )
            }
        }
    }
}
```

### **Шаг 2: Обновить AndroidManifest.xml**

```xml
<service 
    android:name=".MiFitnessNotificationListener"
    android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
    android:exported="true">
    <intent-filter>
        <action android:name="android.service.notification.NotificationListenerService" />
    </intent-filter>
</service>
```

### **Шаг 3: Обработать в React (src/hooks/useMiFitnessNotifications.ts)**

```typescript
export function useMiFitnessNotifications() {
  useEffect(() => {
    const handler = (event: CustomEvent<{ title: string; text: string }>) => {
      console.log('[Mi Fitness]', event.detail);
      
      // Парсим данные
      const { text } = event.detail;
      if (text.includes('steps') || text.includes('шаг')) {
        // Извлекаем количество шагов
      } else if (text.includes('bpm') || text.includes('пульс')) {
        // Извлекаем пульс
      }
    };
    
    window.addEventListener('mi-fitness-notification', handler as EventListener);
    return () => window.removeEventListener('mi-fitness-notification', handler as EventListener);
  }, []);
}
```

---

## 📊 Сравнительная таблица

| Решение | Real-time | Надёжность | Сложность | Размер APK | Статус |
|---------|-----------|------------|-----------|------------|--------|
| **Health Connect** | ❌ (10-30 мин) | ⭐⭐⭐⭐⭐ | ✅ Простая | +0 MB | ✅ Реализовано |
| **NotificationListener** | ⚡ Средне | ⭐⭐⭐⭐ | ✅ Простая | +0 MB | 🆕 Предложено |
| **Web Bluetooth** | ⚡⚡⚡ Да | ⭐⭐⭐ | ✅ Простая | +0 MB | ✅ Реализовано |
| **Gadgetbridge** | ⚡⚡⚡ Да | ⭐⭐⭐⭐ | ❌ Сложная | +2-3 MB | ❌ Не нужно |
| **BLE Direct** | ⚡⚡⚡ Да | ⭐⭐ | ❌ Очень сложная | +1 MB | ❌ Не стабильно |

---

## 🎓 Ключевые выводы

1. **Без паринга нельзя** - все health данные требуют authentication
2. **Health Connect достаточно** - для 90% use cases
3. **NotificationListener** - хорошее дополнение для быстрых событий
4. **Real-time от Xiaomi** - возможен только через Gadgetbridge (сложно)
5. **Ваша специальная команда** - вероятно включает device в "developer mode" для непрерывной BLE передачи

---

## ❓ Вопрос к вам

**Какая проблема с текущей реализацией?**
- Задержка 10-30 минут слишком большая?
- Нужен real-time пульс во время практик?
- Mi Fitness не всегда синхронизируется?

Помогите понять приоритет, чтобы выбрать оптимальное решение! 🚀
