{
  "name": "onda-mcp-analytics",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only MCP server exposing ONDA analytics (GA4 funnel, installs, source health).",
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "google-auth-library": "^9.15.0",
    "jsonwebtoken": "^9.0.2"
  },
  "scripts": {
    "test": "node --experimental-test-module-mocks --test \"test/**/*.test.mjs\""
  }
}
