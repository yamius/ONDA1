# Emoton — build notes (superseded)

> **Moved.** This file's v1 build notes are superseded by the full, current feature
> doc: **[`emoton.md`](emoton.md)**.
>
> The original notes described the pre-merge v1 (a simplified breath pacer in a
> since-deleted `EmotonPractice.tsx`, EN-only). That is out of date: the practice
> branch is now the real ported adaptive practice (`LandingPractice.tsx`), the
> wheel/flow is EN + RU, and all build/CSP/prerender/attribution details live in
> [`emoton.md`](emoton.md). Update that file going forward.
